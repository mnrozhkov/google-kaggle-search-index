# Search Index pipeline: a pipeline to build and test Similarity Index for the embedding model


Overview:
------
`models` - for models
`reports` - for  metrics and plots files 
`src` - for source code shared over projects

Run experiments 
----
```bash 
dvc run exp

```